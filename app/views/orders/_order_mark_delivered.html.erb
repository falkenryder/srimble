<%= simple_form_for @order, url: order_path(@order) do |f| %>
  <%= f.hidden_field :status, :value => "delivered" %>
  <%= f.submit "Mark as Delivered", class: "btn-signup btn btn-primary", data: {turbo_confirm: "All items were delivered for this order?"}%>
<% end %>
